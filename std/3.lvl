Repeating
If you put something in <code>loop{HERE}</code>, JSK-13 will execute it until the end has been reached.
For example, after running the code below JSK-13 will turn left and run until the end.

left
loop {
    run
}

#############
#,###########
#..........F#
#############

run
left
loop {
    run
}